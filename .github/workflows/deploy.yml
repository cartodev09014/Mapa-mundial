name: Deploy del mapa para revisión

on:
  push:
    branches: ["main"] # Se ejecuta cada vez que se suben cambios a la rama principal

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    environment:
          name: github-pages
          url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Crear archivo de configuración config.js desde el Secret
        run: |
          echo "const API_KEY = '${{ secrets.API_KEY }}';" > ./config.js
          echo "Contenido del directorio actual:"
          ls -R # Esto nos mostrará TODO lo que el robot ve antes de subirlo

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v4

      - name: Subir archivos para publicar
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ github.workspace }}

      - name: Desplegar en GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
